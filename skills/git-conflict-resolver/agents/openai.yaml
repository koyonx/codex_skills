interface:
  display_name: "Git Conflict Resolver"
  short_description: "Detect unresolved merge conflicts and list conflicted files"
  default_prompt: "Use $git-conflict-resolver to detect unresolved merge conflicts in this repo."
