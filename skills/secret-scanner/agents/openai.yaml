interface:
  display_name: "Secret Scanner"
  short_description: "Scan files for hardcoded API keys, tokens, and passwords"
  default_prompt: "Use $secret-scanner to scan this repo for hardcoded secrets and report any findings."
